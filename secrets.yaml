default:
  bobs_password:
    # might need a type: key_value thing here to make it extensible
    arn: arn_goes_here
    key: optional_key_to_access_secret
environment:
  dev
  production
cluster:
  dev-eks-cluster2
  
