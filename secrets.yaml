default:
  bobs_password:
    # might need a type: key_value thing here to make it extensible
    secret_id: test/first-secret
    key: password
environment:
  dev
  production
cluster:
  dev-eks-cluster2
  
